# ====================================================================
# US.T Consolidated requirements.txt
# 合并自:
# 1. HaWoR/requirements.txt
# 2. opentouch/requirements.txt
# 3. hamer/setup.py (install_requires & extras_require)
# ====================================================================

# --------------------------------------------------------------------
# 1. Core Frameworks & Data Processing (核心深度学习与数据处理框架)
# --------------------------------------------------------------------
# Note: For CUDA-enabled PyTorch/Torchvision, we recommend installing them first via conda/pip
# (例如: pip install torch==2.1.0 torchvision==0.16.0 --extra-index-url https://download.pytorch.org/whl/cu118)
torch>=2.0
torchvision
numpy==1.26.4
scipy
pandas
h5py>=3.8.0
datasets
transformers
fsspec
dill
joblib
natsort
pulp

# --------------------------------------------------------------------
# 2. Computer Vision & Graphics (计算机视觉与图形渲染)
# --------------------------------------------------------------------
opencv-python
pillow
scikit-image
imageio
imageio[ffmpeg]
pyrender
trimesh
plyfile
supervision
HTML4Vision
easydict

# --------------------------------------------------------------------
# 3. 3D Hand Model & Specialized Rendering (3D 手部模型与特定三维库)
# --------------------------------------------------------------------
smplx==0.1.28
xtcocotools
pycocotools
aitviewer
moderngl-window==2.4.6
pytorch-minimize
evo

# --------------------------------------------------------------------
# 4. Model Architectures & Core Engines (深度学习模型结构与主干引擎)
# --------------------------------------------------------------------
timm
einops
ultralytics
yacs
hydra-core
hydra-submitit-launcher
hydra-colorlog
pyrootutils
mmcv==1.3.9
mmengine==0.10.4

# --------------------------------------------------------------------
# 5. Training, Logging & Utilities (训练、日志与监控实用工具)
# --------------------------------------------------------------------
pytorch-lightning
lightning-utilities
torchmetrics==1.4.0
wandb>=0.15.0
termcolor>=2.3.0
tqdm>=4.65.0
rich
webdataset
loguru
gdown
lapx

# --------------------------------------------------------------------
# 6. VCS/Git Direct Dependencies (直接来自 Git 仓库的依赖)
# --------------------------------------------------------------------
# 建议通过带有额外标记或按顺序手动运行，以下列出以作记录：
detectron2 @ git+https://github.com/facebookresearch/detectron2.git
chumpy @ git+https://github.com/mattloper/chumpy.git
pytorch3d @ git+https://github.com/facebookresearch/pytorch3d.git@stable
torch-scatter==2.1.2
