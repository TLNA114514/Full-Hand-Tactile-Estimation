# Retired Scratch Utilities

The one-off inspection, repair, and migration scripts that previously lived
here were removed after their maintained equivalents moved to `preprocess/`
and `hamer_tactile_ft/`. This placeholder keeps source synchronization able to
delete stale copies on other machines.

Mesh assets under `opentouch/preprocess/scratch/` are unrelated and remain in
active use.
